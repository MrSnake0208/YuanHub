# 库存奖励补录

入口：库存追踪 → 操作历史 → **添加奖励流水** / **导入上报报告**。

## 手动添加

先选择目标子账号，从始终展开的渠道预设中选择来源，填写实际发生时间（自定义日历或输入日期、时刻；页面标明本机时区）。点击道具图标或密探头像添加奖励，重复点击累加；在入账清单中加减或直接输入数量。心纸支持姓名搜索与品质筛选。派遣渠道必须填写实际消耗体力。预览后确认补录。

| 渠道 | 可选奖励 |
| --- | --- |
| 派遣-洛阳 | 鸡炙、麻籽、蛇肉、茱萸、白金币（按 10 的倍数添加） |
| 派遣-寿春 / 据点情报 | 密探心纸 |
| 历练 | 六韬兵书、兵书全卷、兵书残卷及修为进阶材料 |
| 其他奖励 | 自选道具或心纸类型，并填写自定义渠道名称 |

已知渠道自动决定奖励类型；切换渠道时保留兼容的已选条目，移除不适用的奖励并清空体力。所有心纸列表排除 SP 密探；范围校验仅约束手动添加，不改写报告中的原始记录。若选择其他奖励并填写包含“派遣”的自定义渠道，也必须填写体力。

白金币的手动数量控件以 10 为步进，直接输入也必须是 10 的倍数。报告导入不会改写报告中的原始数量，历史报告中的白金币数量按原始记录校验。

补录使用 `reward_delta`，不会将输入转换成库存快照。已有较新库存基线覆盖的旧奖励由后端判定为仅历史，仍可用于获得量统计。

## 导入 MaaY 报告

支持 UTF-8 `DailyRewards*.txt`（包括 BOM、Windows 换行）及库存交换 v2 JSON。点击投递区选择文件或拖入单份报告后自动解析，也可以展开文本输入区粘贴报告。流水预览默认收起原始状态详情与记录 ID，可逐条展开查看。单份报告上限 5 MB，单次提交 1..1000 条流水。

- TXT 按 `MaaYuan 库存记录` 块解析，读取 `#@MaaYInventoryRefV2` 中的原始 ID、账号与体力信息。
- 根据当前后端库存目录将名称还原为道具/密探 ID；不同实体类型按报告原顺序与原记录 ID 对应。
- 保留原发生时间、原记录 ID、渠道、消耗体力和奖励数量。缺少账号时绑定当前选中子账号；已有账号不符时阻止该记录导入。
- 默认选择上报失败的奖励。已成功或状态未知的记录可核对后逐条选择。
- 库存快照明确跳过；如需迁移快照，使用现有「数据交换」入口。
- 未知或有歧义的名称、缺失 ID、截断内容、无效数量、账号/体力标记不一致等保留为错误行。混合记录块有错误时整块阻止导入。

仅预览不会发送写请求。点击确认后复用 `POST /v1/inventory/import`，展示接受、重复、仅历史、已归档数量及后端警告，并刷新当前库存和操作历史、清除获得量缓存。

手动流水的 `effective_at` 会原样发送给服务端。服务端按账号和业务时间重放：如果它早于已有的完整库存快照，或早于对应条目的局部库存基线，快照覆盖范围内的数量不会改变当前库存，但流水仍会保留在历史并可用于时段获得量；未被 `listed` 快照覆盖的条目仍可能影响 current。提交结果中的 `history_only` 反映服务端实际判定，页面在已加载的最新库存快照之前选择时间时会提前提示这一点。

## 重试

服务端以同一账号、同一记录 ID 和正文进行幂等去重。重复导入同一份 TXT 或同一条流水会计入 `duplicates`，不会再次补录；页面在全部重复时显示“记录已存在，未重复补录”。同 ID 不同正文会返回冲突。TXT 内部同一 `record_id` 的重复行也会在预览阶段禁用，避免重复勾选。

网络或服务端错误后，本次请求内容会在当前页面中保留并锁定，只能原样重试；明确的格式、权限或冲突拒绝允许修正。不要为同一条历史报告重新生成记录 ID。手动补录尚未确认成功时应留在页面原样重试，刷新或离开页面会丢失该手动草稿。

功能直接复用现有正式库存接口，无新增后端接口或延后开放的功能开关。
