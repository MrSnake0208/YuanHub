# YuanHub PWA icons

这些图标已替换为 YuanHub 正式 Logo，源文件来自 `/Users/snake/Desktop/YuanHubLogo`。

当前映射：

- `icon-192.png`：由 `125圆角.png` 生成
- `icon-512.png`：由 `125圆角.png` 生成
- `apple-touch-icon.png`：由 `125圆角.png` 生成
- `icon-maskable-512.png`：由 `正方.png` 生成，供系统安全裁切

页面中的品牌标识统一使用 `public/brand/yuanhub-logo.png`，对应不透明的 `125圆角.png`，以保证米色 / 暖白等浅色背景上的对比度与识别度。

代码继续固定引用这些稳定路径，后续无需修改 PWA 安装逻辑。
